{
  "name": "x-download-desktop",
  "version": "1.0.0",
  "description": "Local Electron desktop client for downloading media from X posts.",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "download": "node scripts/download-link.js",
    "check": "node --check electron/main.js && node --check electron/media-archive.js && node --check electron/preload.js && node --check electron/renderer.js && node --check scripts/download-link.js"
  },
  "devDependencies": {
    "electron": "^44.1.1"
  },
  "dependencies": {
    "exifreader": "^4.44.0",
    "ffprobe-static": "^3.1.0"
  }
}

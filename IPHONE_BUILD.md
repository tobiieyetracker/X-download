# 在 iPhone 上对照搭「清洁版」（推荐第一步）

本地 `template/` 里是同结构的未签名文件，签名通了再导入；  
**现在最稳：在 iPhone 新建快捷指令，按下面搭。API 填你自己的。**

## 0. 新建

1. 打开「快捷指令」→ 「+」  
2. 名称：`X下载·自用模板`  
3. 详情里打开：**显示在分享表**  
4. 接受的输入类型勾选：URL、Safari 网页、文本  
5. 「无输入时」→ **获取剪贴板**

## 1. 公共头（所有类型共用）

1. **注释**：写你的 API 约定（可选）  
2. **设置变量**  
   - 变量名：`inputURL`  
   - 值：快捷指令输入

## 2. 菜单

**从菜单中选择**，提示：`选择作品类型`  
菜单项：

- 单视频  
- 多视频  
- 图片  
- GIF  

下面每个「如果」都在对应菜单项里。

---

### A. 单视频

1. **获取 URL 内容**  
   - URL：`https://你的域名/v1/single`（改成你的）  
   - 方法：POST  
   - 请求体：JSON  
   - JSON：`url` = `inputURL`  
   - Headers：`Content-Type: application/json`，`Accept: application/json`  
2. **获取词典值**，键：`url`  
3. **如果**「词典值」**有任何值**  
   1. **获取 URL 内容**（这次下载媒体）：URL = 上一步词典值  
   2. **保存到相册**  
   3. **显示通知**：`✅ 单视频已保存`  
4. **否则**  
   1. **显示通知**：`❌ 单视频解析失败`  
5. **如果结束**

你的 API 建议返回：

```json
{ "url": "https://cdn.example.com/a.mp4" }
```

---

### B. 多视频

1. **获取 URL 内容** → `https://你的域名/v1/multi`，POST JSON `{"url": inputURL}`  
2. **获取词典值**，键：`urls`  
3. **如果**有任何值  
   1. **对每个重复**「词典值」  
      - **获取 URL 内容**：URL = 重复项目  
      - **保存到相册**  
   2. **重复结束**  
   3. 通知：`✅ 多视频已保存`  
4. **否则** → 失败通知  
5. **如果结束**

```json
{ "urls": ["https://.../1.mp4", "https://.../2.mp4"] }
```

---

### C. 图片

同「多视频」，API 换成 `/v1/image`，返回图片直链列表即可。

```json
{ "urls": ["https://.../1.jpg", "https://.../2.jpg"] }
```

---

### D. GIF

同「单视频」，API 换成 `/v1/gif`，键仍用 `url`（直链到 gif/mp4 均可，相册能收就行）。

```json
{ "url": "https://.../a.gif" }
```

---

## 3. 自测顺序

1. 先让你的 API 在电脑上对一条测试链接返回上述 JSON  
2. 手机快捷指令里暂时把「获取 URL 内容」指到该 API  
3. 用分享表从浏览器/X 分享一条链接跑一遍  
4. 四种类型各测一条  

## 4. 和原版的对应关系

| 原版 | 模板 |
|------|------|
| `videoUrl` | `inputURL` |
| 远程 Base64 配置下发接口 | **删掉**，写死你的 4 个 URL |
| 单视频正则抠 mp4 | 改为 API 直接给 `url`（更稳） |
| 多视频/图片 workers | 你的 `/multi` `/image` |
| 地区检测 / 广告 / 更新 | **不实现** |

本地同结构文件：

- `template/workflow.json`  
- `template/workflow.plist`  
- `template/template.shortcut`  

签名方案就绪后，可再谈把占位 URL 换成正式地址并打包。
